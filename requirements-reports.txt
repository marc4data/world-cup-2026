# Notebook execution tooling — only needed to re-render the reports (daily cron's
# report-refresh step and local notebook work). Kept out of requirements.txt so the
# core ingest/CI install stays lean.
nbconvert==7.17.1
nbformat==5.10.4
ipykernel==7.3.0
